<%= form_for(@photo) do |f| %>

    <div class='form-group'>
      <%= f.label :photo %>
      <%= f.file_field :avatar %>
    </div>

  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.submit class: "btn btn-success" %>
<% end %>
